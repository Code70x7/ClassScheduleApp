<?xml version="1.0" encoding="UTF-8"?>
<Application
    x:Class="ClassScheduleApp.App"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!-- 🟣 Safe, minimal resources (no missing StaticResource references) -->
    <Application.Resources>
        <ResourceDictionary>

            <!-- ====== GRADIENTS ====== -->
            <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,0">
                <GradientStop Color="#6C63FF" Offset="0.0" />
                <GradientStop Color="#00C9A7" Offset="1.0" />
            </LinearGradientBrush>

            <LinearGradientBrush x:Key="PrimaryButtonBrush" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#8C6BFF" Offset="0.0"/>
                <GradientStop Color="#6A38FF" Offset="1.0"/>
            </LinearGradientBrush>

            <!-- ====== COLOR PALETTE ====== -->
            <Color x:Key="Primary">#512BD4</Color>
            <Color x:Key="PrimaryDark">#AC99EA</Color>
            <Color x:Key="Accent">#00C9A7</Color>
            <Color x:Key="White">#FFFFFF</Color>
            <Color x:Key="Black">#000000</Color>
            <Color x:Key="BackgroundLight">#F7F8FF</Color>
            <Color x:Key="TextDark">#212121</Color>

            <!-- ====== STYLES ====== -->

            <!-- Page background -->
            <Style TargetType="ContentPage" ApplyToDerivedTypes="True">
                <Setter Property="BackgroundColor" Value="{StaticResource BackgroundLight}" />
                <Setter Property="Padding" Value="0" />
            </Style>

            <!-- Buttons -->
            <Style TargetType="Button" ApplyToDerivedTypes="True">
                <Setter Property="TextColor" Value="{StaticResource White}" />
                <Setter Property="Background" Value="{StaticResource PrimaryButtonBrush}" />
                <Setter Property="CornerRadius" Value="12" />
                <Setter Property="Padding" Value="16,12" />
                <Setter Property="MinimumHeightRequest" Value="44" />
                <Setter Property="MinimumWidthRequest" Value="44" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="Shadow">
                    <!-- Prevent XAML parser crash by using explicit struct syntax -->
                    <Setter.Value>
                        <Shadow Brush="{StaticResource Black}" Opacity="0.25" Offset="0,3" Radius="6" />
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Label / Text styles -->
            <Style TargetType="Label" ApplyToDerivedTypes="True">
                <Setter Property="TextColor" Value="{StaticResource TextDark}" />
                <Setter Property="FontSize" Value="14" />
            </Style>

            <!-- Shell styling -->
            <Style TargetType="Shell" ApplyToDerivedTypes="True">
                <Setter Property="Shell.BackgroundColor" Value="{StaticResource White}" />
                <Setter Property="Shell.ForegroundColor" Value="{StaticResource Black}" />
                <Setter Property="Shell.TitleColor" Value="{StaticResource Black}" />
                <Setter Property="Shell.NavBarHasShadow" Value="False" />
                <Setter Property="Shell.TabBarBackgroundColor" Value="{StaticResource White}" />
                <Setter Property="Shell.TabBarForegroundColor" Value="#D600AA" />
                <Setter Property="Shell.TabBarTitleColor" Value="#D600AA" />
                <Setter Property="Shell.TabBarUnselectedColor" Value="#212121" />
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
