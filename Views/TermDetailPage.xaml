<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ClassScheduleApp.Views.TermDetailPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Edit Term">

    <!-- ========= RESOURCES ========= -->
    <ContentPage.Resources>
        <!-- Gradients -->
        <LinearGradientBrush x:Key="PurpleGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#7C4DFF" Offset="0.0" />
            <GradientStop Color="#5B2DFF" Offset="1.0" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="TealGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#00C9A7" Offset="0.0" />
            <GradientStop Color="#0097B2" Offset="1.0" />
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="DangerGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FF4B5C" Offset="0.0" />
            <GradientStop Color="#8C2BFF" Offset="1.0" />
        </LinearGradientBrush>

        <!-- Shadow resource (MAUI cannot parse "0.25,6,0,3" in a Setter) -->
        <Shadow x:Key="CardShadow"
                Brush="Black"
                Opacity="0.25"
                Radius="6"
                Offset="0,3" />

        <!-- Button style (now using CardShadow) -->
        <Style x:Key="GradientButton" TargetType="Button">
            <Setter Property="TextColor" Value="White" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="CornerRadius" Value="20" />
            <Setter Property="HeightRequest" Value="46" />
            <Setter Property="FontAttributes" Value="Bold" />
            <Setter Property="Padding" Value="16,10" />
            <Setter Property="Shadow" Value="{StaticResource CardShadow}" />
        </Style>
    </ContentPage.Resources>

    <!-- ========= PAGE BODY ========= -->
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="18">

            <!-- TERM TITLE -->
            <Label Text="Term Name" FontAttributes="Bold" FontSize="16" />
            <Entry x:Name="TitleEntry"
                   Placeholder="e.g., Fall 2025 Term"
                   FontSize="16" />

            <!-- START / END DATES -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="16" Margin="0,10,0,0">
                <VerticalStackLayout>
                    <Label Text="Start Date" FontAttributes="Bold" FontSize="14" />
                    <DatePicker x:Name="StartDatePicker" />
                </VerticalStackLayout>

                <VerticalStackLayout Grid.Column="1">
                    <Label Text="End Date" FontAttributes="Bold" FontSize="14" />
                    <DatePicker x:Name="EndDatePicker" />
                </VerticalStackLayout>
            </Grid>

            <!-- SAVE / DELETE BUTTONS -->
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="10" Margin="0,12,0,4">
                <Button Text="Save"
                        Clicked="Save_Clicked"
                        Background="{StaticResource PurpleGradient}"
                        Style="{StaticResource GradientButton}" />
                <Button Grid.Column="1"
                        Text="Delete"
                        Clicked="Delete_Clicked"
                        Background="{StaticResource DangerGradient}"
                        Style="{StaticResource GradientButton}" />
            </Grid>

            <!-- COURSES SECTION -->
            <Label Text="Courses"
                   FontAttributes="Bold"
                   FontSize="18"
                   Margin="0,14,0,0" />

            <!-- ADD COURSE BUTTON -->
            <Button Text="Add Course"
                    Clicked="AddCourse_Clicked"
                    Background="{StaticResource TealGradient}"
                    Style="{StaticResource GradientButton}" />

            <!-- COURSE LIST -->
            <CollectionView x:Name="CoursesCollection"
                            SelectionMode="Single"
                            SelectionChanged="Collection_ClearSelection"
                            Margin="0,10,0,30">

                <CollectionView.EmptyView>
                    <Label Text="No courses yet. Tap 'Add Course' to create one."
                           HorizontalOptions="Center"
                           TextColor="#777"
                           FontSize="14"
                           Margin="0,10" />
                </CollectionView.EmptyView>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border StrokeShape="RoundRectangle 16"
                                StrokeThickness="0"
                                BackgroundColor="#F7F8FA"
                                Margin="0,6"
                                Padding="10">

                            <Grid ColumnDefinitions="*,Auto,Auto" ColumnSpacing="8">
                                <!-- COURSE INFO -->
                                <VerticalStackLayout>
                                    <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="16" />
                                    <Label FontSize="12"
                                           Text="{Binding StartDate, StringFormat='Start: {0:MMM d, yyyy}'}" />
                                    <Label FontSize="12"
                                           Text="{Binding EndDate, StringFormat='End: {0:MMM d, yyyy}'}" />
                                </VerticalStackLayout>

                                <!-- EDIT -->
                                <Button Grid.Column="1"
                                        Text="Edit"
                                        CommandParameter="{Binding .}"
                                        Clicked="EditCourse_Clicked"
                                        Background="{StaticResource PurpleGradient}"
                                        Style="{StaticResource GradientButton}"
                                        WidthRequest="80" />

                                <!-- DELETE -->
                                <Button Grid.Column="2"
                                        Text="Delete"
                                        CommandParameter="{Binding .}"
                                        Clicked="DeleteCourse_Clicked"
                                        Background="{StaticResource DangerGradient}"
                                        Style="{StaticResource GradientButton}"
                                        WidthRequest="80" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
